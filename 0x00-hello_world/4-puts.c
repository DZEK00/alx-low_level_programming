#include <stdio.h>
/**
  * The main function is the entry point of the program. It is responsible for
 * initializing the program and executing the necessary tasks. In this program,
 * the main function reads input from the user, performs some calculations, and
 * displays the results to the screen. The program then exits after the user
 * presses a key.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
/*This return value ends the program */
return (0);
}
